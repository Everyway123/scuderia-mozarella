{
  "name": "scuderia-mozarella",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "SCUDERIA MOZARELLA — F1-менеджер у браузері, сезон 2026",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "race": "node scripts/race.ts",
    "balance": "node scripts/balance.ts",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "verify": "tsc --noEmit && vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^26.1.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}
